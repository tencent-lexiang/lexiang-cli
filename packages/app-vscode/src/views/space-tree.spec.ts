// Note: This test file is intentionally minimal.
// The space-tree.ts module imports 'vscode' which is not available in Node.js test environment.
// Full testing requires VS Code Test Framework (vscode-test).
//
// For now, we keep this file as a placeholder to maintain the .spec.ts naming convention.
// The actual functionality should be tested manually or via integration tests.

import { describe, expect, test } from 'vitest';

describe('space-tree', () => {
  test('placeholder test to ensure file is valid', () => {
    expect(true).toBe(true);
  });
});
